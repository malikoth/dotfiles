if [ $CURR_SHELL = zsh ]; then

    setopt NO_DOT_GLOB
    setopt NULL_GLOB
    setopt EXTENDED_GLOB

    setopt KSH_GLOB
    setopt NO_HIST_VERIFY
    setopt CSH_JUNKIE_HISTORY

else

    shopt -u dotglob
    shopt -s nullglob
    shopt -s extglob


fi
