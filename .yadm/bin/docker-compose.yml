version: '2'

services:
  devenv:
    build:
      context: .
      dockerfile: Dockerfile
    image: malikoth/devenv
    network_mode: host
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
